<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
			<h1> Tasks... </h1> 
                {{#each tasks}}
            <input type="hidden" name="id" value="{{ this._id }}">
            <ul>
                <li> {{this.task}} </li>
                <li> {{this.label}} </li>
                <li> {{this.status}} </li>
                <a href="/tasks/{{ this._id }}">Delete</a>
                <a href="/tasks/update/{{ this._id }}">Update</a>
            <br> 
            </ul>						
                {{/each}}
</body>
</html>